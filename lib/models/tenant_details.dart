class TenantDetails {
  final int timeStamp;
  final int reading;
  final int unit;
  final int total;

  TenantDetails(
      {this.timeStamp = 0, this.reading = 0, this.unit = 0, this.total = 0});
}
